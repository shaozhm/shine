PROCEDURE "sap.hana.democontent.epm.procedures::getPOItems" (
    OUT ex_addresses "sap.hana.democontent.epm.data::PurchaseOrder.Item" )
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   --DEFAULT SCHEMA <default_schema_name>
   READS SQL DATA AS
BEGIN
   /*************************************
       Write your procedure logic 
   *************************************/
   ex_addresses = 
     select *
              from "sap.hana.democontent.epm.data::PurchaseOrder.Item";

END